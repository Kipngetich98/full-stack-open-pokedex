name: Hello World
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Hello world
      run: echo "Hello World!"
    - name: Print date
      run: date
    - name: Print directory contents
      run: ls -l

